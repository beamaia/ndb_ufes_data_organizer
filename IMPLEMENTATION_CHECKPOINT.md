# Implementation Checkpoint

Date: 2026-07-06

## Objective

Implement the accepted documentation, figures, and MkDocs experience upgrade for the NDB-UFES Data Organizer documentation site.

## Current Status

Implementation is ready for local review after the graph performance and readability pass. The site should not be committed or published until the user explicitly approves the final local version. The latest correction pass made the graph-heavy pages preview-first, fixed long-axis readability in the generated Plotly charts, and validated the built MkDocs site.

## Accepted Scope

- Keep MkDocs as the documentation stack for this pass.
- Audit MkDocs Material features and compatible plugins before final styling changes.
- Improve the Labcin-inspired visual design without using an oversized logo or white logo background.
- Make public docs point readers to Mendeley for dataset access.
- Keep DVC/AWS S3 documentation maintainer-only.
- Rewrite semicolon-heavy inline prose into topic-style sections, tables, and short paragraphs.
- Rework Phase 1, Phase 2, and Phase 3 result pages so each page explains its own result and links to the next result page.
- Convert Phase 1, Phase 2, and Phase 3 implementation pages toward package-style function/class documentation.
- Add reproducible wiki/thesis figure generation with Labcin palette support.
- Expand fold structure documentation with percentages.
- Rework contamination documentation around the Origin 0011 example, conservative status, smaller visuals, and the implemented similarity method.

## Completed

- Confirmed the working tree already has staged Labcin branding changes:
  - `docs/assets/branding/labcin-logo.png`
  - `docs/assets/branding/labcin-mark.png`
  - `docs/styles/labcin.css`
  - `mkdocs.yml`
- Confirmed current docs stack uses MkDocs Material with sidebar sections, top button, code copy, and search suggestions.
- Audited the current MkDocs setup and chose a low-risk enhanced Material approach for this pass.
- Did not add mkdocstrings in this pass because the code is still script-oriented and automatic imports would make the Pages build more fragile.
- Added reusable documentation figure styling in `scripts/src/docs/figure_style.py`.
- Added `scripts/generate_wiki_figures.py`.
- Generated current documentation/thesis figures:
  - `docs/assets/generated/dataset_diagnosis_summary.png`
  - `docs/assets/generated/source_task_counts.png`
  - `docs/assets/generated/top_cramers_v.png`
  - `docs/assets/generated/phase2_model_selection.png`
  - `docs/assets/generated/fold_origin_patch_percent.png`
  - `docs/assets/generated/fold_diagnosis_percent.png`
  - `docs/assets/contamination/origin_0011_overlap_bbox.png`
  - `docs/visualizations/generated/*.html`
  - `results/thesis_figures/*.png`
  - `results/thesis_figures/*.svg`
  - `results/thesis_figures/*.pdf`
- Updated MkDocs navigation for Phase 2 Results and Phase 3 Results.
- Updated the homepage, setup page, status page, factsheet, data dictionary, result pages, code pages, fold page, contamination page, exploratory page, visualization index, reproducibility page, pipeline overview, and README.
- Reworked contamination documentation around Origin 0011 and the image-registration overlap method.
- Replaced the initial ORB match-line figure with a translation-only overlap bounding-box figure for `p0020` and `p0021`.
- Removed the superseded ORB snippet PNG, CSV, SVG, and PDF generated artifacts.
- Reworked public dataset access language so Mendeley is public-facing and DVC/AWS S3 is maintainer-only.
- Continued the local website-polish pass after restart on 2026-07-06.
- Cleaned the contamination page so the public method centers the overlap-rectangle figure rather than the earlier ORB-line artifact.
- Rewrote the data dictionary fold-file section around the canonical `results/phase3_fold_creation/` outputs and the actual current CSV schemas.
- Reduced the factsheet from a long uncaptained notebook-image gallery to a curated public summary with explained generated figures.
- Added figure-panel captions to the exploratory analysis figures that remain in the public site.
- Updated local documentation instructions to open `http://127.0.0.1:8000/ndb_ufes_data_organizer/`.
- Tightened CSS for mobile wrapping and reduced the contamination snippet height cap.
- Turned the visualizations page into a direct link index for representative Phase 1 plots and generated result plots.
- Added the Labcin ResearchGate attribution link to the MkDocs Material footer/social links.
- Added `docs/dataset-figure-gallery.md`, generated by `scripts/generate_wiki_figures.py`, with inline embedded Plotly counterparts for all prior dataset-statistics figures.
- Expanded `scripts/generate_wiki_figures.py` so the previous static dataset-statistics figures now have generated Plotly HTML counterparts under `docs/visualizations/generated/`.
- Made Plotly HTML generation deterministic by using stable div IDs based on each figure stem.
- Updated factsheet citation language to say the Python package does not exist yet and that current users should cite the repository/fold files as a data-organization layer.
- Removed remaining "fold package" wording from the factsheet identity section.
- Updated contamination wording so Origin 0011 is clearly an example demonstrating patch-overlap leakage risk, not the full contamination case set.
- Added concrete status verification dates to Phase 1, Phase 2, and Phase 3 status language.
- Renamed implementation-page API headings to function/class/helper headings where the code is not a published package API.
- Regenerated Exploratory Analysis as the full inline Plotly dataset-statistics page instead of a short static-image subset.
- Added fold-level Plotly and thesis/static outputs for diagnosis, morphology cluster, gender, age group, and combined-stratum spread.
- Updated Fold Structure so stratification is shown visually, not only described in prose.
- Added concrete exploratory insights to the factsheet, including matched-subset scope, patch/origin count differences, high `Not informed` metadata rates, and dysplasia-severity missingness.
- Added a home-page AI-assisted documentation note for Codex/GPT-5 under human-written instructions and human validation before publication.
- Switched Phase 2 Results, Phase 3 Results, and Factsheet summary figures from static wiki images to embedded Plotly views while keeping static exports for thesis use.
- Added `docs/javascripts/plotly-loader.js` and registered it in `mkdocs.yml`.
- Converted the full Exploratory Analysis and Dataset Figure Gallery pages from eager Plotly iframes to lazy cards:
  - 53 static Plotly PNG previews per page.
  - 0 eager iframes on each graph-heavy page.
  - Interactive Plotly charts load in place only after pressing **Load Interactive Figure**.
- Added Plotly PNG preview generation under `docs/assets/generated/plotly_previews/`.
- Added `kaleido` to the docs extra so Plotly previews can be regenerated reproducibly.
- Added `--skip-plotly-previews` for fast page-only generation and `--export-plotly-thesis` for full Plotly PNG/SVG/PDF thesis exports when needed.
- Reworked the generated source-task-count chart from facet rows to a simple horizontal task/class bar chart.
- Reworked long-label dataset charts to horizontal or wrapped layouts:
  - source-paper task counts,
  - top Cramer's V associations,
  - source metadata vs matched subset diagnosis,
  - count/percent task distributions,
  - missingness charts,
  - row/column percentage association charts,
  - lesion-size-by-diagnosis.
- Humanized generated labels such as `TaskII` and `source_origin_count`.
- Added a dedicated Phase 2 tuning diagnostics page:
  - `docs/phase2-tuning-diagnostics.md`
  - 55 lazy Plotly cards across the current Phase 2 model set.
  - Elbow curves, inertia-by-PCA curves, silhouette-by-K curves, silhouette heatmaps, and top-10 configuration charts for each current model.
  - Beginner-friendly explanations for PCA, K, inertia, silhouette, heatmaps, top-10 rankings, and fold-readiness rejection.
  - `docs/assets/generated/phase2_tuning_diagnostics.csv`
  - `docs/assets/generated/phase2_tuning_top10_configurations.csv`
- Updated Phase 2 silhouette heatmaps so every finite heatmap cell shows the numeric mean silhouette value directly on the plot, with contrast-aware text color.
- Added Phase 2 tuning diagnostics to the MkDocs navigation under Implementation > Phase 2.
- Updated Phase 2 Results so the single selection chart links to the full tuning diagnostics page.
- Removed the redundant generated `Demographic And Clinical Distributions` overview card because it repeated the individual demographic and clinical charts that follow it.

## Next Steps

1. Do not commit yet. A commit triggers website deployment, and the local site is still being reviewed.
2. Review the local website at `http://127.0.0.1:8000/ndb_ufes_data_organizer/`, especially:
  - [Exploratory Analysis](http://127.0.0.1:8000/ndb_ufes_data_organizer/exploratory-analysis/),
  - [Dataset Figure Gallery](http://127.0.0.1:8000/ndb_ufes_data_organizer/dataset-figure-gallery/),
  - [Dataset Factsheet](http://127.0.0.1:8000/ndb_ufes_data_organizer/factsheet/),
  - [Fold Structure](http://127.0.0.1:8000/ndb_ufes_data_organizer/fold-structure-explained/).
3. Review whether the unstaged `pyproject.toml` and `uv.lock` changes should be included in the eventual docs commit. They now include the earlier `pathspec<1` change plus the new `kaleido` docs extra needed for Plotly preview regeneration.
4. Leave `PROJECT_RECAP.md`, `old_copilot_stuff/`, `labcin`, and `labcin2` unstaged unless they are intentionally archived.
5. After local approval, stage the currently untracked generated preview and loader assets that belong to the website:
  - `docs/assets/generated/plotly_previews/`
  - `docs/javascripts/plotly-loader.js`
  - `docs/phase2-tuning-diagnostics.md`
  - `docs/visualizations/generated/phase2_elbow_*.html`
  - `docs/visualizations/generated/phase2_inertia_by_pca_*.html`
  - `docs/visualizations/generated/phase2_silhouette_by_k_*.html`
  - `docs/visualizations/generated/phase2_silhouette_heatmap_*.html`
  - `docs/visualizations/generated/phase2_top10_configurations_*.html`
  - `docs/assets/generated/phase2_tuning_diagnostics.csv`
  - `docs/assets/generated/phase2_tuning_top10_configurations.csv`
6. Open areas include deeper figure narration, package-style function/class docs once the scripts are converted into importable modules, and contamination validation after the human-in-the-loop review resumes.

## Shutdown Handoff

Updated before desktop shutdown on 2026-07-05 at 20:43:10 -03.

- Do not commit after restart until the local website has been reviewed and explicitly approved for publication.
- Most documentation and generated-figure changes are currently staged from the earlier commit-ready pass.
- The user rejected an elevated `git restore --staged .` request, so the staged index was intentionally left untouched.
- `PROJECT_RECAP.md`, `labcin`, `labcin2`, and `old_copilot_stuff/` remain untracked and should stay outside the publish commit unless deliberately selected later.
- Resume by running `uv run --extra docs mkdocs serve`, reviewing the site locally, then continuing page-by-page polish.
- Current local server command used after restart: `uv run --extra docs mkdocs serve -a 127.0.0.1:8000`.

## Blockers

- None.

## Validation Status

- `uv run --extra docs python scripts/generate_wiki_figures.py` passed and generated 284 figure artifacts after removing the redundant demographic/clinical overview chart.
- `uv run --extra docs python -m py_compile scripts/generate_wiki_figures.py scripts/src/docs/figure_style.py` passed.
- `uv run python -m unittest discover -s tests -v` passed: 7 tests.
- Markdown link validation passed for public Markdown links.
- Built-site asset validation passed: 0 missing local asset references.
- Built graph-page audit passed:
  - Exploratory Analysis has 0 eager iframes, 52 lazy cards, and 52 preview images.
  - Dataset Figure Gallery has 0 eager iframes, 52 lazy cards, and 52 preview images.
  - Phase 2 Tuning Diagnostics has 0 eager iframes, 55 lazy cards, and 55 preview images.
  - Phase 2 silhouette heatmap HTML contains visible numeric cell labels.
  - `docs/javascripts/plotly-loader.js` is present in the built site.
- Built HTML audit confirmed no Material tab navigation markup.
- Phase 2/3 artifact sanity check passed: selected model is Virchow, origin `0` is retained, 203 origins and 3,086 patches are assigned, six folds exist, no origin leakage is present, and patch ratio is 1.02745.
- `uv run --extra docs mkdocs build --strict` passed.
- `git diff --check` passed.
- `git diff --cached --check` passed. SVG trailing-whitespace cleanup is now part of `scripts/generate_wiki_figures.py`.

## Validation Notes

- MkDocs prints the upstream Material for MkDocs warning about future MkDocs 2.0 compatibility. The strict build still exits successfully.
- No MkDocs tabs feature is enabled. Navigation remains sidebar/mobile drawer based.
- The current contamination figure highlights a 266 x 257 pixel shared rectangle between `p0020` and `p0021` with normalized cross-correlation 1.000.
- Full Plotly PNG/SVG/PDF thesis export is intentionally not part of the default figure-generation path because exporting 50+ figures in all formats is slow. Use `uv run --extra docs python scripts/generate_wiki_figures.py --export-plotly-thesis` only when those full Plotly thesis copies are needed.
